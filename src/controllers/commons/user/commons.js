const express = require('express');
const router = express.Router();

router.get('/:object', (req, res) =>{
// the object parameter will be replaced with the user routes eg. courses, track and user details.
})


module.exports = router;